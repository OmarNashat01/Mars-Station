1- The input files require an extra number in the first line which determines if the speed of the rovers will be entered for each different rover or for all rovers of the same type (1 same speed for the same type, 2 different speed for every rover).

2- Output files can be differet for the same input files because there is a chance that a mission fails.

